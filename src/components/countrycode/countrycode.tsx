import { FC } from "react";
import styles from "./countrycode.module.scss";
const ConutryCode: FC = () => {
  return <div className={`${styles.country_code_container}`}> +91 </div>;
};

export default ConutryCode;
